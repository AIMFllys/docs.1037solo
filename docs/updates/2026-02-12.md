# 📅 2026-02-12 更新日志

### 【面向用户】

#### v2.3.085 🛡️ 修复了若干已知问题

#### v2.3.086 🔐 账号注册功能上线
现在可以使用邮箱注册 1037 SOLO 账号了！注册页面配有图形验证码防止机器人刷号，并通过邮件验证确认身份。同时支持忘记密码时通过邮件找回。

#### v2.3.087 🛡️ 修复了若干已知问题

#### v2.3.088 ✨ 登录页面全面焕新 + 管理后台上线
登录页面图像焕然一新，新增流星雨、飘落粒子等视觉特效，科技感大幅提升。同时上线了管理员后台，可查看平台使用数据、管理用户反馈。

---

### 【面向开发者】

#### v2.3.085 🔧 v2.3.084 稳定性补丁
- **变更**：代码文件计数与 v084 完全相同（464 个），属于 Perplexify 子系统完成后的稳定化提交（Bug 修复或细节调整），无新文件产生。

#### v2.3.086~v2.3.087 🔐 认证体系扩展（注册 + 忘记密码）
新增核心认证页面组件：

| 新增文件 | 职责 |
|---------|------|
| `components/auth/RegisterPage.tsx` | 完整注册流程：邮箱格式校验、密码强度验证、图形验证码、邮件激活提示 |
| `components/auth/ForgotPasswordPage.tsx` | 忘记密码流程：输入邮箱 → 发送重置链接 → 提示查收邮件 |
| `components/auth/CaptchaInput.tsx` | 图形验证码输入组件，防止批量注册 |

**后端支撑（`server/solo-api/`）：**

| 新增文件 | 职责 |
|---------|------|
| `server/solo-api/src/utils/emailService.js` | 阿里云 DirectMail SMTP 邮件发送服务，负责注册验证邮件和密码重置邮件模板生成与发送 |
| `server/solo-api/apply_schema.js` | 数据库 Schema 初始化脚本，用于一键部署时建表 |

#### v2.3.088 🎨 管理员后台 + UI 特效库（最大单版本变更）

**管理后台（6 个文件）：**

| 新增文件 | 职责 |
|---------|------|
| `components/admin/AdminLayout.tsx` | 管理员页面整体布局（侧边栏 + 内容区域） |
| `components/admin/AdminSidebar.tsx` | 管理侧边栏导航（用户管理 / 成本仪表盘 / 反馈审核） |
| `components/admin/CostDashboard.tsx` | AI 调用成本仪表盘，可视化各模型的使用量与费用统计 |
| `components/admin/UserManagementPanel.tsx` | 用户管理面板：查看注册用户、调整调用限额、封禁/解封账号 |
| `components/admin/FeedbackReviewPanel.tsx` | 用户反馈审核面板：浏览、标记、处理用户提交的反馈内容 |
| `components/pages/AdminDashboard.tsx` | 管理员仪表盘页面（路由入口） |
| `components/pages/AdminLogin.tsx` | 管理员专属登录页（独立于普通用户登录） |

**AI 功能增强：**

| 新增文件 | 职责 |
|---------|------|
| `components/features/ai/components/FeedbackPrivacyModal.tsx` | 用户反馈前的隐私声明弹窗，确认用户同意后才发送反馈数据 |

**UI 特效组件库新增（3 个）：**

| 新增文件 | 效果描述 |
|---------|---------|
| `components/ui/meteors.tsx` | 流星雨动效：随机生成多条流星从右上至左下飞过，用于登录页视觉背景 |
| `components/ui/falling-pattern.tsx` | 飘落粒子效果：Canvas 绘制的字符/图形粒子随机从上方飘落，增加科技氛围感 |
| `components/ui/gooey-text-morphing.tsx` | 粘性文字变形效果：SVG Filter + CSS 实现的胶状文字变形动画 |

- **文件规模**：487 个文件（+14 TSX, +2 JS, 净增 23 个代码文件）。
- **这是整个 v2.3 系列单版本增量最大的一次提交**，完成了用户认证体系和管理后台的完整闭环。