# 📅 2026-01-31 更新日志（v2.3.092 测试体系建立）

> **版本说明**：v2.3.092 的文件夹创建时间为 2026-01-31，早于 v2.3.067（2026-02-05）。这反映了版本快照保存顺序与版本号编排的不同步，v092 实际上是该版本在 1 月时已存档的一个较成熟状态，包含了最完整的前端测试覆盖。

### 【面向用户】

#### v2.3.092 🛡️ 修复了若干已知问题

---

### 【面向开发者】

#### v2.3.092 🧪 前端全栈测试套件建立（最成熟版本）
v092 在 v091 基础上新增了完整的前端自动化测试体系，共 **22 个测试文件**，覆盖核心功能的各个层级。

**测试目录结构（位于 `frontend/tests/`）：**

```
frontend/tests/
├── _helpers/
│   ├── render.tsx          # 自定义测试渲染器，集成 React Query / Zustand Provider
│   └── testEnv.ts          # 测试环境初始化（mock fetch、环境变量等）
├── api/
│   ├── client.test.ts      # Axios 实例测试：CSRF Token 注入、401 重试逻辑
│   └── csrf.test.ts        # CSRF Token 获取、缓存与清除逻辑测试
├── lib/
│   ├── logger.test.ts      # 日志系统测试
│   └── rateLimiter.test.ts # 客户端请求频率限制器测试
├── components/ai/
│   └── AIChatLayout.test.tsx # AI 对话主界面集成测试（含 SSE 流式模拟）
├── pages/
│   ├── App.test.tsx                  # 应用根组件路由测试
│   ├── AccountPage.test.tsx          # 账户页面功能测试
│   ├── AdminDashboard.test.tsx       # 管理员仪表盘测试
│   ├── AdminLogin.test.tsx           # 管理员登录测试（权限校验）
│   ├── ExplorePage.test.tsx          # 探索页数据加载测试
│   ├── NotFoundPage.test.tsx         # 404 页面渲染测试
│   ├── RedeemManagementPage.test.tsx # 兑换码管理页测试
│   ├── SearchPage.test.tsx           # 搜索页工作流测试
│   └── admin/
│       ├── CostModelPage.test.tsx    # 成本预测页面测试
│       └── DocsPredictionPage.test.tsx # 文档预测页面测试
├── services/
│   └── streamChatBp.test.ts         # AI 流式对话服务测试（SSE 解析、错误处理）
└── utils/
    ├── helpers.test.ts      # 通用工具函数测试
    ├── parser.test.ts       # Markdown / 引用解析工具测试
    ├── urlSafety.test.ts    # URL 安全检查（防止跳转到恶意地址）
    └── validation.test.ts   # 输入校验工具测试
```

**测试策略分析：**

| 测试层 | 测试文件数 | 覆盖重点 |
|--------|-----------|---------|
| API 层 | 2 | CSRF 机制、Axios 拦截器 |
| 工具库 | 2 | 日志、限流 |
| 组件层 | 1 | AI 对话界面集成 |
| 页面层 | 11 | 路由、权限、数据加载 |
| 服务层 | 1 | SSE 流式响应 |
| 工具函数 | 4 | 解析、安全、校验 |

- **文件规模**：489 个文件（122 TSX + 128 TS，较 v091 增加 +12 TSX, +10 TS，全部为测试文件）。
- **架构亮点**：`frontend/` 目录的引入是一次目录重构的信号，测试代码迁入独立 `frontend/tests/` 命名空间，与主业务代码解耦。
- **v092 总结**：这是整个 v2.3 系列已知快照中**代码质量最高、测试覆盖最完整**的版本，为后续向 `src/` 架构迁移奠定了基础。